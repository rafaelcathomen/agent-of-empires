appearance = "light"

background = "#eff1f5"
border = "#bcc0cc"
terminal_border = "#04a5e5"
selection = "#dce0e8"
session_selection = "#ccd0da"
title = "#1e66f5"
text = "#4c4f69"
dimmed = "#acb0be"
hint = "#209fb5"
running = "#40a02b"
# Light theme: darker = more attention. Waiting takes the deepest peach,
# fresh_idle the mid amber, idle the lightest slate so the row visibly
# fades into the page over time.
waiting = "#fe640b"
fresh_idle = "#df8e1d"
idle = "#9ca0b0"
unread = "#7287fd"
error = "#d20f39"
terminal_active = "#1e66f5"
# Light theme: darker/more saturated endpoints than the dark builtins so the
# ramp stays legible on the pale background. Hot is a deep burnt orange
# distinct from the peach waiting and the pink-red error; cold is a muted
# slate-blue darker than dimmed, never the saturated subagent blue.
subagent_active = "#3a5ccc"
heat_hot = "#c4380d"
heat_cold = "#6c7398"
group = "#179299"
search = "#7287fd"
accent = "#fe640b"
diff_add = "#40a02b"
diff_delete = "#d20f39"
diff_modified = "#df8e1d"
diff_header = "#04a5e5"
help_key = "#df8e1d"
branch = "#04a5e5"
sandbox = "#8839ef"

[syntax]
shiki_theme = "catppuccin-latte"
